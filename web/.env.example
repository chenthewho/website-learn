# 前端环境变量示例（web）
#
# 默认无需任何配置：开发期所有以 /api 开头的请求由 vite.config.ts 的
# server.proxy 透明转发到后端 http://localhost:4000。生产构建后由部署层
# 反向代理 /api 到后端即可，前端始终以相对路径 /api 调用。
#
# 仅当你确实需要让前端直连一个非同源的后端地址时，才取消下面注释并填写。
# 注意：Vite 只暴露以 VITE_ 前缀的变量给客户端代码。
#
# VITE_API_BASE_URL=http://localhost:4000
